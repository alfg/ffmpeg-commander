# ts-jest@27 has a peer dep on typescript <5.0, but we intentionally use TS 5.x.
# ts-jest is installed only to satisfy a require.resolve() check in @vue/vue2-jest;
# babel-jest is the actual TypeScript transformer at runtime (see jest.config.js).
legacy-peer-deps=true
